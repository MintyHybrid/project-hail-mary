% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_manuscript.R
\name{theme_manuscript_bg}
\alias{theme_manuscript_bg}
\title{Background-only overlay for chrome-free ggplot themes}
\usage{
theme_manuscript_bg()
}
\value{
A ggplot2 theme object (partial).
}
\description{
A partial \code{theme()} covering \code{plot.background}, \code{panel.background} and
the legend background/key, set to transparent so figures show whichever
page theme is active rather than a baked-in paper colour. Meant to be
layered after visualization-specific themes that intentionally hide
axes/grid (\code{ggplot2::theme_void()}, \code{ggtree::theme_tree()}/
\code{theme_tree2()}, \code{ggraph::theme_graph()}, \code{ggseqlogo::theme_logo()}) --
those set both background elements to \code{element_blank()} (transparent) as
part of their own construction, so overriding \code{plot.background} alone
still leaves the PNG device's own background (and, for plots with a
legend, the legend's own opaque background) showing through. Legend text
and title are also recoloured to the same neutral \code{text_strong} used by
\code{\link[=theme_manuscript]{theme_manuscript()}}, since their default black would be illegible once
the legend background stops being paper-coloured on the dark theme.
Use \code{theme_manuscript()} instead for ordinary axis-bearing plots.
}
