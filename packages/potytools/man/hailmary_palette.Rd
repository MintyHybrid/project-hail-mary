% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_manuscript.R
\name{hailmary_palette}
\alias{hailmary_palette}
\title{Cyanotype colour palette}
\usage{
hailmary_palette()
}
\value{
Named character vector: \code{paper}, \code{ink}, \code{soft}, \code{accent}, \code{flag},
\code{line}, \code{text_strong}, \code{text_soft}.
}
\description{
Named hex colours matching the book's \code{theme-light.scss} CSS custom
properties, for notebooks that want to opt a \verb{scale_*_manual()} call into
the site palette rather than picking ad hoc hex values. \code{text_strong} and
\code{text_soft} are a separate, deliberately neutral pair (see
\code{\link[=theme_manuscript]{theme_manuscript()}}) -- not simply \code{ink}/\code{soft} -- because static
R-rendered figures are baked once at render time and can't react to the
light/dark toggle the way the site's own CSS can; \code{ink}/\code{soft} are tuned
for the light paper background specifically and would be close to
illegible against the dark theme's paper colour.
}
