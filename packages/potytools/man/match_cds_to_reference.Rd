% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_genbank.R
\name{match_cds_to_reference}
\alias{match_cds_to_reference}
\title{Match a reference sequence to its CDS feature and extract the coding sequence}
\usage{
match_cds_to_reference(record, name, ref_protein = NULL, min_similarity = 0.7)
}
\arguments{
\item{record}{A single parsed GenBank record (one element of the list
returned by \code{\link[=parse_genbank_file]{parse_genbank_file()}} or \code{\link[=load_genbank_folder]{load_genbank_folder()}}).}

\item{name}{Character scalar. Reference sequence name to match, e.g. a
protein-alignment FASTA header containing a \code{protein_id}.}

\item{ref_protein}{Character scalar. Reference amino-acid sequence (gaps
allowed), used only for the similarity fallback. Optional.}

\item{min_similarity}{Numeric in \verb{[0, 1]}. Minimum fractional amino-acid
identity (over the shorter of the two sequences) required to accept a
similarity-based match. Default \code{0.7}.}
}
\value{
A \code{Biostrings::DNAString} with the matched CDS (reverse-complemented
if the feature is on the minus strand), or \code{NULL} if no feature matched or
\code{record$sequence} is unavailable.
}
\description{
Finds the CDS in a parsed GenBank \code{record} (as returned by one element of
\code{\link[=parse_genbank_file]{parse_genbank_file()}}'s result) that corresponds to a named reference
sequence, trying in order: (1) an exact \code{protein_id} match against \code{name},
(2) a normalised \code{product}-qualifier match, and (3) — if \code{ref_protein} is
supplied — the CDS whose translation is most similar to \code{ref_protein},
accepted only above \code{min_similarity}.
}
