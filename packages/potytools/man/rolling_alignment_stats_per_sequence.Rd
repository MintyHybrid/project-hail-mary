% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rolling_stats.R
\name{rolling_alignment_stats_per_sequence}
\alias{rolling_alignment_stats_per_sequence}
\title{Per-sequence sliding-window composition statistics}
\usage{
rolling_alignment_stats_per_sequence(
  aln,
  window = 300,
  step = 30,
  codon_aligned = TRUE,
  remove_gaps = FALSE
)
}
\arguments{
\item{aln}{Character matrix (rows = sequences, columns = sites), e.g.
\code{as.matrix(Biostrings::readDNAStringSet(...))}.}

\item{window}{Window width in columns (default 300).}

\item{step}{Step between window starts, in columns (default 30).}

\item{codon_aligned}{If \code{TRUE} (default), \code{window} and \code{step} must be
multiples of 3 so codon positions line up.}

\item{remove_gaps}{If \code{TRUE}, gap characters are excluded from the metric
denominators (composition computed over aligned bases only).}
}
\value{
Data frame, one row per sequence x window, with a leading
\code{sequence} column plus \code{window}, \code{start}, \code{end}, \code{GC}, \code{GC3}, \code{CpG_OE},
\code{entropy}, \code{gap_fraction}, \code{GC_pos1}, \code{GC_pos2}, \code{GC_pos3}.
}
\description{
As \code{\link[=rolling_alignment_stats]{rolling_alignment_stats()}} but computes the metrics separately for each
sequence (row) of the alignment, so isolate-to-isolate variation across the
alignment can be plotted as a multi-line trace. Also vectorised per sequence.
}
