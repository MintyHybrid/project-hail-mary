% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rolling_stats.R
\name{rolling_alignment_stats}
\alias{rolling_alignment_stats}
\title{Sliding-window composition statistics across an alignment}
\usage{
rolling_alignment_stats(
  aln,
  window = 300,
  step = 30,
  codon_aligned = TRUE,
  remove_gaps = FALSE
)
}
\arguments{
\item{aln}{Character matrix (rows = sequences, columns = sites), e.g.
\code{as.matrix(Biostrings::readDNAStringSet(...))}.}

\item{window}{Window width in columns (default 300).}

\item{step}{Step between window starts, in columns (default 30).}

\item{codon_aligned}{If \code{TRUE} (default), \code{window} and \code{step} must be
multiples of 3 so codon positions line up.}

\item{remove_gaps}{If \code{TRUE}, gap characters are excluded from the metric
denominators (composition computed over aligned bases only).}
}
\value{
Data frame, one row per window, with columns \code{window}, \code{start},
\code{end}, \code{GC}, \code{AT}, \code{GC3}, \code{CpG_OE}, \code{gap_fraction}, \code{entropy},
\code{GC_pos1}, \code{GC_pos2}, \code{GC_pos3}.
}
\description{
Slides a window along the columns of a nucleotide alignment and, pooling all
sequences within each window, computes GC content, AT content, GC at the
third codon position (GC3), the CpG observed/expected ratio, Shannon entropy,
gap fraction, and per-codon-position GC. Useful for mapping regional
compositional bias (e.g. CpG depletion, GC3 wobble adaptation) along a genome
or gene alignment. Implemented with vectorised prefix sums (fast on
genome-length alignments).
}
